on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n myappv1 -g myappv1a9e9-rg --location "East US" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n myappv1 --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n myappv1 --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n myappv1 --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: myappv1.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t myappv1.azurecr.io/keshavtest1:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push myappv1.azurecr.io/keshavtest1:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_keshavtest1_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep myappv1 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace myappv1`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: myappv1
        container-registry-url: myappv1.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: keshavtest1dockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: myappv1
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          myappv1.azurecr.io/keshavtest1:${{ github.sha }}
        imagepullsecrets: |
          keshavtest1dockerauth   
